# supervalu-prototype

Prototype workspace for the SuperValu store management tooling.

## Structure

```
.
├── README.md
└── .gitignore
```

## Getting started

Static prototype — no build step yet. Serve the folder locally:

```bash
python3 -m http.server 8000
```

Then open http://localhost:8000

## Notes

The existing production site lives in a separate repo
(`aarondoyle2026.github.io`) and is not tracked here.
